% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FlexRecordLinkage.r
\name{SurvivalUnstable}
\alias{SurvivalUnstable}
\title{Exponential survival function for PIV instability}
\usage{
SurvivalUnstable(Xlinksk, alphask, times)
}
\arguments{
\item{Xlinksk}{Matrix (n linked records x (1 + n covariates)), first column:
intercept, remaining columns: covariates used to model instability.}

\item{alphask}{Numeric vector, hazard coefficients matching the columns of \code{Xlinksk}.}

\item{times}{Numeric vector, time gap between the linked records.}
}
\value{
Numeric vector, probability that the true PIV value is unchanged
for each linked pair.
}
\description{
For a linked pair \verb{(i, j)} from A and B, models the probability that the
true value of an unstable PIV coincides as
\verb{P(HAik = HBjk | t_ij) = exp(-exp(X . alpha) * t_ij)}. To use a different
survival model, adapt this function together with \code{\link[=loglikSurvival]{loglikSurvival()}}.
}
\examples{
cov_k <- cbind(intercept = rep(1, 5))
times <- c(0.001, 0.2, 1.3, 1.5, 2)
SurvivalUnstable(cov_k, alphask = log(0.28), times = times)
}
